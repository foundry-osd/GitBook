# Machine naming

Use Machine naming to define how a computer name is selected during deployment.

## Naming requirements

Foundry Deploy validates names as 1–15 characters containing letters, numbers, or hyphens.

{% hint style="warning" %}
Choose a naming method that prevents duplicate names and matches directory, inventory, and device-management requirements.
{% endhint %}

## Configure naming

1. Open **Customization > Machine naming**.
2. Enable the required naming behavior.
3. Configure the computer-name prefix, choose whether the suffix is generated automatically, and decide whether the deployment operator can edit the suffix.
4. Return to **Start** and confirm customization readiness.

<figure>
  <img src="../../.gitbook/assets/foundry-osd-customization-machine-naming-01-configuration.png" alt="Foundry OSD machine naming configuration">
  <figcaption>Configure the computer-name prefix, generated suffix, and operator editing behavior.</figcaption>
</figure>
